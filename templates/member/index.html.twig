{% extends 'base.html.twig' %}
{% block title %}My Pets
{% endblock %}
{% block body %}

	{# {{dump(pets)}} #}
    <div class="min-h-screen py-12 px-4 sm:px-6 lg:px-8">
	<div class="max-w-6xl mx-auto">
		<div class="mb-8">
			<h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-6">
				Members
			</h1>
			<div class="flex flex-wrap gap-2 sm:gap-4 items-center overflow-x-auto pb-2 scrollbar-hide">
				<a href="#" class="filter-btn px-5 py-2 rounded-full bg-(--primary-color) text-white font-semibold text-xs uppercase tracking-wider">
					ALL
				</a>
				<a href="#" class="filter-btn px-4 py-2 rounded-full text-gray-600 dark:text-gray-400 hover:text-(--primary-color) dark:hover:text-(--primary-color) font-semibold text-xs uppercase tracking-wider transition-colors">
					DOGS
				</a>
				<a href="#" class="filter-btn px-4 py-2 rounded-full text-gray-600 dark:text-gray-400 hover:text-(--primary-color) dark:hover:text-(--primary-color) font-semibold text-xs uppercase tracking-wider transition-colors">
					CATS
				</a>
				<a href="#" class="filter-btn px-4 py-2 rounded-full text-gray-600 dark:text-gray-400 hover:text-(--primary-color) dark:hover:text-(--primary-color) font-semibold text-xs uppercase tracking-wider transition-colors">
					REPTILES
				</a>
				<a href="#" class="filter-btn px-4 py-2 rounded-full text-gray-600 dark:text-gray-400 hover:text-(--primary-color) dark:hover:text-(--primary-color) font-semibold text-xs uppercase tracking-wider transition-colors">
					FISH
				</a>
				<a href="#" class="filter-btn px-4 py-2 rounded-full text-gray-600 dark:text-gray-400 hover:text-(--primary-color) dark:hover:text-(--primary-color) font-semibold text-xs uppercase tracking-wider transition-colors">
					BIRDS
				</a>
				<a href="#" class="filter-btn px-4 py-2 rounded-full text-gray-600 dark:text-gray-400 hover:text-(--primary-color) dark:hover:text-(--primary-color) font-semibold text-xs uppercase tracking-wider transition-colors">
					RODENTS
				</a>
				<a href="#" class="filter-btn px-4 py-2 rounded-full text-gray-600 dark:text-gray-400 hover:text-(--primary-color) dark:hover:text-(--primary-color) font-semibold text-xs uppercase tracking-wider transition-colors">
					HORSES
				</a>
				<a href="#" class="filter-btn px-4 py-2 rounded-full text-gray-600 dark:text-gray-400 hover:text-(--primary-color) dark:hover:text-(--primary-color) font-semibold text-xs uppercase tracking-wider transition-colors">
					INSECTS
				</a>
				<a href="#" class="filter-btn px-4 py-2 rounded-full text-gray-600 dark:text-gray-400 hover:text-(--primary-color) dark:hover:text-(--primary-color) font-semibold text-xs uppercase tracking-wider transition-colors">
					OTHER
				</a>
			</div>
		</div>
		<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {% for pet in pets %}
                {% include 'member/member.html.twig' with {'pet': pet} %}
            {% endfor %}
		</div>
		<div class="mt-12 text-center">
			<button class="cursor-pointer text-(--primary-color) font-semibold hover:underline flex items-center justify-center mx-auto">
				Load more ....
			</button>
		</div>
	</div>
	</div>
{% endblock %}
